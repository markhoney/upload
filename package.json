{
	"name": "upload-test",
	"version": "1.0.1",
	"description": "A test of uploading an Excel spreadsheet, manipulating the data, and then allowing download of the edited data as a spreadsheet",
	"author": "Mark Honeychurch <mark@honeychurch.org> (https://mark.honeychurch.org)",
	"license": "ISC",
	"scripts": {
		"dev": "vite",
		"build": "vite build",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"dependencies": {
		"vue": "^3.0.0-rc.1"
	},
	"devDependencies": {
		"vite": "^1.0.0-rc.1",
		"@vue/compiler-sfc": "^3.0.0-rc.1"
	}
}
